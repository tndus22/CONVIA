# Optional. Used only when fetching a Slack channel through the integration UI.
# You can also enter the token directly in the app instead of storing it here.
SLACK_BOT_TOKEN="xoxb-your-token"

# Optional server port (default: 3000)
PORT="3000"
